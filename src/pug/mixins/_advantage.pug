<i class="zmdi zmdi-city"></i>
<i class="zmdi zmdi-mood"></i>
mixin advantage(title, desc, iconClass)
	.advantage&attributes(attributes)
		.advantage__title #{title}
		.advantage__description #{desc}
		.advantage__icon
			i(class=iconClass).icon-color